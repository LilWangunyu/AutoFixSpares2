{
  "name": "autofix-functions",
  "description": "Cloud Functions for AutoFix Spare Parts (process orders, notify devices)",
  "engines": { "node": "18" },
  "main": "index.js",
  "scripts": {
    "lint": "eslint . || true",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^11.11.0",
    "firebase-functions": "^4.4.0"
  }
}
